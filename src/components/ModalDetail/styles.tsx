import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  contentContainer: {
    width: '100%',
    borderRadius: 12,
  },
  textCenter: {
    textAlign: 'center',
  },
});

export default styles;
